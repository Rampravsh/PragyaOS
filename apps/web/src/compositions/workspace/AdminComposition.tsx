/**
 * Admin workspace page composition.
 * Connects page containers to admin tools, RBAC panels, and organization settings.
 */
export function AdminComposition(): React.JSX.Element {
  return (
    <div className="admin-composition">
      <h1>Admin</h1>
    </div>
  );
}

export default AdminComposition;
