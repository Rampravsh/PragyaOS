/**
 * Login page composition.
 * Connects page containers to authentication features and forms.
 */
export function LoginComposition(): React.JSX.Element {
  return (
    <div className="login-composition">
      <h1>Login</h1>
    </div>
  );
}

export default LoginComposition;
