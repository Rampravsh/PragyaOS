import { spacing } from "../tokens/spacing";

export const editorialSpacing = {
  stack: spacing[8], // spacing between sections
  inline: spacing[6], // spacing inside grid items
  paragraph: spacing[4], // spacing between paragraphs
  outerContainer: spacing[12], // spacing of main outer layout bounds
};
